parentElement
├── prepend()  → inserts as FIRST child INSIDE parent
│   └── ...existing children...
└── append()   → inserts as LAST child INSIDE parent

element.before() → inserts as sibling ABOVE the element
[element itself]
element.after()  → inserts as sibling BELOW the element