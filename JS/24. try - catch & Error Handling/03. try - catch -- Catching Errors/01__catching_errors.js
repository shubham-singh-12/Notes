// ============================================================ try / catch -- Catching Errors ============================================================

// ============================ SYNTAX ============================
try {
    // risky code that might throw an error
} catch (error) {
    // code that runs if an error occurs
}
