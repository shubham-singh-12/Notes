// =================================================================== TERNARY OPERATOR ===================================================================

var age = 18;
var result = age >= 18 ? "Eligible" : "Not Eligible";
console.log(result);                    // Eligible


var student_Exam_Score = 50;
var student_Result = student_Exam_Score >= 60 ? "Student Passes" : "Student Failed";
console.log(student_Result);                     // Student Failed